name: fibs
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.6
  path: ^1.9.1
  flutter_tts: ^4.2.2
  yaml: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - data/groups.yaml
    - data/nouns.json
    - data/verbs.json
    - data/objectives.json
    - data/naturalization.json
